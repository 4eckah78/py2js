// comment at the beggining
// another comment at the beggining
// Test global vars
var a = [1, "assdf", '3', true]; // test array
var g = a.pop(); // test array method
var b = 15; // test array method int
var C = "sdf"; // test str
var A = true; // test bool
// test funcs

function __f123_get(a, strange_arg, b = 10) { 
    console.log(a.length);
    // test print
    // test operations
    a += 0;
    a *= 10.32432400;
    a %= 5;
    let g = (5 + 2) / 2;
    c[i % 2] = 2;
    let d = a[k];
    let sum = a[0];
    let sum2 = a[b[35 * a - 152 / 2 ** 2]];
    let b = true;
    // test cycles
    for (let i = 0; i < a.length; i++) {
        console.log(i);
    }
    for (let j = 2; j < 35; j+=2) {
        // some comment inside for
    }
    let k = 0;
    let sum = -k;
    while (true) {
        k *= 5;
        if (((!k) || ((k <= jkh) && (var != l))) || ((k > 1000) && (((jk == 'Hello') || (a.length >= 36))))) {
            break;
        }
        else {
            a /= -35;
            sum += 3223 * a ** (2 * hs) / 32 + count_somethimg(25, k, eps = 0.01);
        }
    }
    // some another comment inside func
    __f123_get();
    // test return
    return ~k + 25 >> 2;
}
// comment between fuctions declarations


function fdfgdf() { // comment at func header
    let a = true;
    let string = "sdfdsf";
    __f123_get();
    if ((a == 0) || (!b == false)) {
        do_something(arg1, arg2 = "hello", arg3 = a);
    }

    for (let i = 5; i < 10; i++) {
        if (i % 2 == 0) {
            continue;
        }

    }
    // test complicated if
    if (a == "bad") {
        bad();
    }
    else if (a == 'good'){
        good();
    }
    else if (true == false){
        never_executed(infinity().length, joke = "haha");
    }
    else {
        default();
    }
    console.log(Array.fromfib(10));
}
// test real func


function* fib(n) { 
    let a = 0;
    let b = 1;
    for (let i = 0; i < n; i++) {
        yield a;
        let t = a;
        let a = b;
        let b = t + b;
    }
}
// comment at the end
