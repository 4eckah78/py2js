# comment at the beggining

# another comment at the beggining 
# Test global vars

a = [1, "assdf", '3', True] # test array
g = a.pop() # test array method
b = 15 # test array method int
C = "sdf" # test str
A = True # test bool

# test funcs

def __f123_get(a, strange_arg, b=10):
    print(len(a)) # test print
    # test operations
    a += 0
    a *= 10.32432400
    a %= 5
    g = (5 + 2) / 2
    c[i % 2] = 2
    d = a[k]
    sum = a[0]
    sum2 = a[b[35 * a - 152 / 2**2]]
    b = True
    # test cycles
    for i in range(len(a)):
        print(i)
    for j in range(2, 35, 2):
        pass
        # some comment inside for

    k = 0
    sum = -k
    while True:
        k *= 5
        if not k or k <= jkh and var != l or k > 1000 and (jk == 'Hello' or len(a) >= 36):
            break
        else:
            a /= -35
            sum += 3223 * a**(2*hs) / 32 + count_somethimg(25, k, eps=0.01)

    # some another comment inside func
    __f123_get()
    # test return
    return ~k + 25 >> 2 

# comment between fuctions declarations

def fdfgdf(): # comment at func header
    a = True
    string = "sdfdsf"
    __f123_get()
    if a == 0 or not b == False:
        do_something(arg1, arg2="hello", arg3=a)
    for i in range(5, 10):
        if i % 2 == 0:
            continue
    # test complicated if
    if a == "bad":
        bad()
    elif a == 'good':
        good()
    elif True == False:
        never_executed(len(infinity()), joke="haha")
    else:
        default()
    print(list(fib(10)))

# test real func
def fib(n):
    a = 0
    b = 1
    for i in range(n):
        yield a
        t = a
        a = b
        b = t + b

# comment at the end
